<!DOCTYPE html>
<html>
  <head>
    <title>StockAuction</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>

    <h1>Leilão de Galpões</h1>
    <nav>
      <%= link_to 'Cadastrar-se', new_user_registration_path %>
      <%= link_to 'Logar', new_user_session_path %>
      
      <% if user_signed_in? %>
        <%= current_user.email %>
      <% end %>
      
    </nav>

    
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
